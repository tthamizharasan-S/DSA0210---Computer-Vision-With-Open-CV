import cv2
import numpy as np
from google.colab.patches import cv2_imshow

img = cv2.imread("/content/exp 20.jpeg", 0)
blur = cv2.GaussianBlur(img, (9,9), 10)

k = 2.5  # boost factor
highboost = cv2.addWeighted(img, k, blur, -(k-1), 0)

cv2_imshow(highboost)
cv2.waitKey(0)
